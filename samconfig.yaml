version: 0.1
default:
  build:
    parameters:
      manifest: './package.json'
      build_in_source: 'true'
      cached: 'true'
      parallel: 'true'
  deploy:
    parameters:
      capabilities: CAPABILITY_IAM
      image_repositories: []